name: executor_test
version: 0.0.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  ffi: ^2.1.0
  test: ^1.22.0
  core:
    path: ../../../core/dart/
  memory:
    path: ../../../memory/dart/
  executor:
    path: ../../dart/
  collection: ^1.18.0
